info:
  name: searchmovie
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/v1/search?q=family man
  params:
    - name: q
      value: family man
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
